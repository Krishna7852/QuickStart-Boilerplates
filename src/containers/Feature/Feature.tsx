import * as React from "react";

interface IFeatureProps {}

const Feature: React.FunctionComponent<IFeatureProps> = (props) => {
  return (
    <div>
      <h1>Feature component Work..!!</h1>
    </div>
  );
};

export default Feature;
